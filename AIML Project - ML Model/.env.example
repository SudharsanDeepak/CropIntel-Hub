# MongoDB Connection
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/market_analyzer

# Admin API Key for protected endpoints
ADMIN_API_KEY=cropintelhub_admin

# API Keys for real-time data sources
# Get Agmarknet API key from: https://data.gov.in/
AGMARKNET_API_KEY=your_agmarknet_api_key_from_data_gov_in

# USDA API key (optional - public endpoints work without key)
# Get from: https://mymarketnews.ams.usda.gov/mars-api
USDA_API_KEY=your_usda_api_key_optional

# Weather API (OpenWeatherMap) - REQUIRED for accurate price predictions
# Get free API key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key

# Data update schedule (in hours)
# Set to 0.0833 for 5-minute updates (real-time)
# Set to 1 for hourly updates
# Set to 24 for daily updates (recommended for production)
DATA_UPDATE_INTERVAL=24

# Note: Weather data is critical for price predictions!
# If OpenWeather API fails, the system uses seasonal averages as fallback.
